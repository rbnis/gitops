apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patchesStrategicMerge:
  - helmrelease.yaml

patches:
  - target:
      version: v1
      kind: Namespace
      name: observability
    path: namespace.yaml
