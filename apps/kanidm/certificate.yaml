apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-rbn-is
spec:
  secretName: auth-rbn-is-tls
  commonName: auth.rbn.is
  dnsNames:
    - auth.rbn.is
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
