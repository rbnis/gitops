apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: kanidm-custom-css
    files:
      - override.css
  - name: kanidm-custom-images
    files:
      - background.webp
